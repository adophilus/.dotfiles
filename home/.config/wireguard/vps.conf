[Interface]
# PrivateKey is NOT here — injected at up-time by PostUp from the
# sops-decrypted file (see users/adophilus/home.nix). This conf is
# secret-free. Flake-managed source of truth — edit here + darwin-rebuild,
# never ~/.config/wireguard/vps.conf (a symlink into the store).
Address = 10.100.0.3/32
PostUp = wg set %i private-key /Users/adophilus/.config/sops-nix/secrets/adophilus/wg-nadir-key

[Peer] # contabo (vps)
PublicKey = eFRbH1eD2p8QJidX2FhWX7pQdla/ret2akO9l7x5JGg=
Endpoint = 37.60.224.206:51820
AllowedIPs = 10.100.0.0/24
PersistentKeepalive = 25
